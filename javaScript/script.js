// JavaScript to toggle accordion functionality on mobile
const navLinks = document.querySelector(".nav-links");

navLinks.addEventListener("click", () => {
  navLinks.classList.toggle("active");
});
